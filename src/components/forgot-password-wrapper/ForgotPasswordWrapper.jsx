import React from "react";

import "./ForgotPasswordWrapper.scss";

export default function ForgotPasswordWrapper({ children }) {
  return <div className="forgot-password-wrapper">{children}</div>;
}
